import { createContext } from "react";

export const UserContext = createContext();

/*Step 01:
* createContext using createContext()
use Provider property of created context and pass children in it
export createdContext and created ContextProvider
*/